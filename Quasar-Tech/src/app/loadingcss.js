import { StyleSheet } from "react-native";

export const estilos = StyleSheet.create({
    logo: {
        alignSelf: "center",
        marginTop: "-60%",
        flex: 1,
        width: "60%",
        resizeMode: "contain",        
    }
});

export default estilos;